# Miyzamchi 2.0 Parser Microservice — Cloud Run (Python 3.11)
# Версии закреплены ниж. границей; при сборке зафиксируйте точные через pip freeze.
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
python-multipart>=0.0.9        # обязателен для UploadFile / multipart-формы
docling>=2.7,<3.0             # IBM Docling: парсинг PDF -> Markdown в памяти
